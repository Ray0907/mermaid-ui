import { describe, it, expect } from 'vitest'

// Dummy test to ensure CLI test suite runs
describe('CLI package', () => {
    it('has at least one test', () => {
        expect(true).toBe(true)
    })
})
